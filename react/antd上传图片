
const eles = list.map(ele => (
    <li className="ele" key={ele.name}>
        <Upload {...uploadProps}>
            <Icon type="picture" className="ico" />
            <p>{ele.desc}</p>
        </Upload>
    </li>
));
